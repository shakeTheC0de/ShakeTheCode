let alfabeto ="".split("");
let veces;

alfabeto.forEach(letra) => {
    veces = Math.floor(Math.random) * 30);
    console.log();
    process.stdout.write(letra + "\t" + veces + "veces:\t");
    imprime(letra, veces);
});